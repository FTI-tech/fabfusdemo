
import { useState } from "react";
import Image from "next/image";

const MainPreview = ({ design }: { design: string }) => {
  return (
    <div className="flex-grow flex justify-center items-center bg-gray-100 p-4">
     hello
    </div>
  );
};

export default MainPreview;
